export default KSite;
declare function KSite(props: any): JSX.Element;
declare namespace KSite {
    namespace propTypes {
        const optionalColors: PropTypes.Requireable<object>;
        const accentColor: PropTypes.Requireable<string>;
    }
}
import PropTypes from "prop-types";
